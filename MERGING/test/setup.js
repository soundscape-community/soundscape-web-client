// Import turfjs so that test scripts have it in their global namespace,
// just like when it gets imported via the <script> tag in the browser.
import * as turf from '@turf/turf';
global.turf = turf;
