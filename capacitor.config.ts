import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'services.soundscape.multiplatform',
  appName: 'SoundscapeMultiplatform',
  webDir: 'dist'
};

export default config;
